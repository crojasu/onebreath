<h1>WELCOME</h1>

<% @presets.each do |preset| %>
  <br><%= link_to preset.name, preset_timer_sessions_path(preset), method: :post  %>
  </br><br>
   <%= link_to "Edit", edit_preset_path(preset) %>
<% end %>
<br>

<%= link_to "+", new_preset_path  %>

<%= render 'shared/navbar' %>
